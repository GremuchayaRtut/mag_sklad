version: "3.9"

services:
  app:
    volumes: []
    restart: always
    deploy:
      replicas: 2
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 2

  db:
    ports: []
    restart: always

  nginx:
    ports:
      - "80:80"
      - "443:443"
    restart: always
